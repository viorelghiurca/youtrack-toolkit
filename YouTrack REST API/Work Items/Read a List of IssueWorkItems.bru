meta {
  name: Read a List of IssueWorkItems
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/workItems?$top=3&fields=created,duration(presentation,minutes),author(name),creator(name),date,id
  body: none
  auth: inherit
}

params:query {
  $top: 3
  fields: created,duration(presentation,minutes),author(name),creator(name),date,id
}

settings {
  encodeUrl: true
}
