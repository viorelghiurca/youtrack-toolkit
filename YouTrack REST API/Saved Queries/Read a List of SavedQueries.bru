meta {
  name: Read a List of SavedQueries
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/savedQueries?$top=3&fields=id,name,query,owner(login,name),visibleFor(name,id),issues(idReadable,summary)
  body: none
  auth: inherit
}

params:query {
  $top: 3
  fields: id,name,query,owner(login,name),visibleFor(name,id),issues(idReadable,summary)
}

settings {
  encodeUrl: true
}
