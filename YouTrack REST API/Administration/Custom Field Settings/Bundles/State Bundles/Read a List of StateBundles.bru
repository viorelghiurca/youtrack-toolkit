meta {
  name: Read a List of StateBundles
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/customFieldSettings/bundles/state?fields=name,id,values(name,id,ordinal,isResolved),isUpdateable
  body: none
  auth: inherit
}

params:query {
  fields: name,id,values(name,id,ordinal,isResolved),isUpdateable
}

settings {
  encodeUrl: true
}
