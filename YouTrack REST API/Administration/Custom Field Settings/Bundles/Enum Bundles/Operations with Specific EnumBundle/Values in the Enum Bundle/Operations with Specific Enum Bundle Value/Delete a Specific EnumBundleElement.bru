meta {
  name: Delete a Specific EnumBundleElement
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/admin/customFieldSettings/bundles/enum/:id/values/:enumBundleElementId
  body: none
  auth: inherit
}

params:path {
  id: {{enumBundleID}}
  enumBundleElementId: {{enumBundleElementID}}
}

settings {
  encodeUrl: true
}
