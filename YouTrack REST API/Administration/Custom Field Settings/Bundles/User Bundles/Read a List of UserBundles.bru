meta {
  name: Read a List of UserBundles
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/customFieldSettings/bundles/user?fields=name,id,isUpdateable,individuals(login,id,name),groups(name,id),aggregatedUsers(name,id,login)
  body: none
  auth: inherit
}

params:query {
  fields: name,id,isUpdateable,individuals(login,id,name),groups(name,id),aggregatedUsers(name,id,login)
}

settings {
  encodeUrl: true
}
