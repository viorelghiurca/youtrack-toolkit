meta {
  name: Read a List of UserGroups
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/customFieldSettings/bundles/user/:id/groups?fields=name,id,usersCount,icon,allUsersGroup,teamForProject(name)
  body: none
  auth: inherit
}

params:query {
  fields: name,id,usersCount,icon,allUsersGroup,teamForProject(name)
}

params:path {
  id: {{userBundleID}}
}

settings {
  encodeUrl: true
}
