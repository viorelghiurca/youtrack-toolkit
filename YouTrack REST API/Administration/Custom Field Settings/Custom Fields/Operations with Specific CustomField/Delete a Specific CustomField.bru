meta {
  name: Delete a Specific CustomField
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/admin/customFieldSettings/customFields/:id
  body: none
  auth: inherit
}

params:path {
  id: {{customFieldID}}
}

settings {
  encodeUrl: true
}
