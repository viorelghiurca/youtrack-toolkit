meta {
  name: Update the System Work Time Settings
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/timeTrackingSettings/workTimeSettings?fields=id,name,workDays,firstDayOfWeek,minutesADay
  body: json
  auth: inherit
}

params:query {
  fields: id,name,workDays,firstDayOfWeek,minutesADay
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "workDays": [1,2,3,4,5],
    "firstDayOfWeek": 1,
    "minutesADay":450
  }
}

settings {
  encodeUrl: true
}
