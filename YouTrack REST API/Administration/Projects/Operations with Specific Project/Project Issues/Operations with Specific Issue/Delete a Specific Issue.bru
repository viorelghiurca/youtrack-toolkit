meta {
  name: Delete a Specific Issue
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/admin/projects/:id/issues/:issueId
  body: none
  auth: inherit
}

params:path {
  id: {{projectID}}
  issueId: {{issue}}
}

settings {
  encodeUrl: true
}
