meta {
  name: Delete a Specific Agile
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/agiles/:id
  body: none
  auth: inherit
}

params:path {
  id: {{agileBoardID}}
}

settings {
  encodeUrl: true
}
