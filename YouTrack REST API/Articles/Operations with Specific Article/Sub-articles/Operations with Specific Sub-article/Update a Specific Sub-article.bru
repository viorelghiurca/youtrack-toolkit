meta {
  name: Update a Specific Sub-article
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/articles/:id/childArticles/:articleId?fields=id,idReadable,summary
  body: json
  auth: inherit
}

params:query {
  fields: id,idReadable,summary
}

params:path {
  id: {{articleID}}
  articleId: {{subArticleID}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "summary": "The Tutorial for the Feature Development"
  }
}

settings {
  encodeUrl: true
}
