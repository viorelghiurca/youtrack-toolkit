meta {
  name: Add a New Reaction
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/issues/:issueId/comments/:commentId/reactions?fields=id,author(login),reaction
  body: json
  auth: inherit
}

params:query {
  fields: id,author(login),reaction
}

params:path {
  issueId: 
  commentId: 
}

body:json {
  {
    "author": {
      "login": "minnie.terry",
      "id": "{{userID}}",
      "$type": "User"
    },
    "reaction": "thanks"
  }
}

settings {
  encodeUrl: true
}
