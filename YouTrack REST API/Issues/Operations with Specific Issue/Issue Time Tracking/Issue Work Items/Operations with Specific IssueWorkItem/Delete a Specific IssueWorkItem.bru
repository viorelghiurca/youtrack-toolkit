meta {
  name: Delete a Specific IssueWorkItem
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/issues/:id/timeTracking/workItems/:issueWorkItemId
  body: none
  auth: inherit
}

params:path {
  id: {{issue}}
  issueWorkItemId: {{workItemID}}
}

settings {
  encodeUrl: true
}
