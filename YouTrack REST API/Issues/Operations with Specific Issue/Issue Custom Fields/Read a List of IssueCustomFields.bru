meta {
  name: Read a List of IssueCustomFields
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/issues/:id/customFields?fields=id,name,value(id,name)
  body: none
  auth: inherit
}

params:query {
  fields: id,name,value(id,name)
}

params:path {
  id: {{issue}}
}

settings {
  encodeUrl: true
}
